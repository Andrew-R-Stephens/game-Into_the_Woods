package models.states.menus.mainmenu;

import props.prototypes.states.menu.AMenu;

public class MenuModel extends AMenu {

    @Override
    public void update(float delta) {
        super.update(delta);
    }
}
