package models.textures.meshes.platform;

import java.awt.image.BufferedImage;

public class PlatformCornerTile extends APlatformTile {

    public PlatformCornerTile(BufferedImage[] layers) {
        super(layers);
    }
}
